"""Reusable, parametrized drawing-sheet templates.

Distinct from `symbols/`: a symbol is a small reusable component (a
resistor, a door swing) inserted at a point in a drawing; a template is a
whole-sheet layout (a border plus a title block) that a drawing is built
on top of. See `library.py` for what's actually built.
"""
